# Future Migrations

Author: CJ Shane

Put new intentional database changes here before applying them to Supabase or another live Postgres database.

Use clear timestamped names, for example:

```text
20260506120000_add_new_reference_table.sql
```

After applying a migration, regenerate `sql/schema.sql` with `scripts/export_live_schema.ps1`.
